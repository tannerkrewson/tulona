export { AppIcon, iconRegistry, type AppIconProps } from './AppIcon';
export {
  getIconMetadata,
  iconCatalog,
  iconNames,
  isEmoji,
  isIconName,
  isIconValue,
  normalizeIconName,
  normalizeIconValue,
  parseIconName,
  type IconValue,
  type IconMetadata,
  type IconName,
} from './icon-names';
