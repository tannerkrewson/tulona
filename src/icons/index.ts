export { AppIcon, iconRegistry, type AppIconProps } from './AppIcon';
export {
  getIconMetadata,
  iconCatalog,
  iconNames,
  isIconName,
  normalizeIconName,
  parseIconName,
  type IconMetadata,
  type IconName,
} from './icon-names';
